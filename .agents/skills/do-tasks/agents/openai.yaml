interface:
  display_name: "Do Tasks"
  short_description: "Execute tmp/tasks.md with dependency ordering"
  default_prompt: "Read tmp/tasks.md and complete all possible tasks in dependency order, marking each completed item as - [x]. Defer only tasks requiring human input and ask all remaining questions together at the end."
