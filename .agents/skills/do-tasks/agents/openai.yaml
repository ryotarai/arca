interface:
  display_name: "Do Tasks"
  short_description: "Execute tmp/tasks/ with DAG + parallelism"
  default_prompt: "Read task files under tmp/tasks/, analyze dependencies, execute ready tasks in parallel (using sub-agents where effective), and move each completed markdown file to tmp/tasks-done/."
